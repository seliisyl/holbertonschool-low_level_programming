#include "3-calc.h"

/**
 * op_add - add 2 entiers
 * @a: le 1er entier
 * @b: le 2d entier
 * Return: som of a et b
 */
int op_add(int a, int b)
{
	return (a + b);
}

/**
 * op_sub - sub 2 entiers
 * @a: le 1eer entier
 * @b: le 2d entier
 * Return: sub a + b
 */
int op_sub(int a, int b)
{
	return (a - b);
}

/**
 * op_mul - multiplicatÂ 2 entiers
 * @a: le 1er entier
 * @b: le 2d entier
 * Return: mult of a et b
 */
int op_mul(int a, int b)
{
	return (a * b);
}

/**
 * op_div - division 2 entiers
 * @a: le 1er entier
 * @b: le 2d entier
 * Return: div of a et b
 */
int op_div(int a, int b)
{
	return (a / b);
}

/**
 * op_mod - modulo 2 entiers
 * @a: le 1er entier
 * @b: le 2d entier
 * Return: mod of a et b
 */
int op_mod(int a, int b)
{
	return (a % b);
}
