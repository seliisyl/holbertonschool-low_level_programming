#include "main.h"
#include <stdio.h>

/**
 * _strlen - Calcule la longueur d'une chaîne de caractères.
 * @s: La chaîne de caractères dont la longueur est à calculer.
 *
 * Return: Le nombre de caractères dans la chaîne.
 */
int _strlen(char *s)
{
	size_t len;

	for (len = 0; s[len] != '\0'; len++)
	{
	}
	return (len);
}
